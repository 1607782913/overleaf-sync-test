% =============================================================
% TDSC published-look starter (visual style close to IEEE Xplore)
% NOTE: IEEE may apply its own production typesetting after acceptance.
% For final submission, also compare with the current IEEE Template Selector.
% =============================================================
\documentclass[10pt,journal]{IEEEtran}

\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{cite}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage[caption=false,font=footnotesize]{subfig}
\usepackage{url}

\begin{document}

\title{Active Diagnostic Probing for Backdoor Sample Cleansing in Contrastive Learning}

\author{First~Author,~\IEEEmembership{Graduate Student Member,~IEEE,}
        Second~Author,~\IEEEmembership{Member,~IEEE,}
        and~Third~Author,~\IEEEmembership{Senior Member,~IEEE}%
\thanks{First Author and Second Author are with the School of XXX, XXX University, City, Country. E-mail: xxx@xxx.edu.}%
\thanks{Third Author is with the Department of XXX, XXX University, City, Country.}%
\thanks{Manuscript received Month xx, 2026; revised Month xx, 2026.}}


\maketitle

\begin{abstract}
Contrastive learning (CL) learns transferable representations from large-scale unlabeled data, but its reliance on weakly curated pre-training datasets makes it vulnerable to data-poisoning backdoor attacks. We study poisoned-sample detection and cleansing for unlabeled CL datasets. Our framework incorporates a probe-transformed trusted reference set into unsupervised encoder training. After training, the same fixed, pre-trained semantic probe is applied to the training samples, and the normalized backbone features of the transformed training and reference samples are jointly embedded with UMAP. DBSCAN is then applied to the training embeddings, and samples outside the largest non-noise cluster are flagged as suspicious under a minority-poisoning assumption. Preliminary BYOL experiments show strong poisoned-sample separation for SSL-Backdoor and CTRL on CIFAR; however, the detector fails to recover CorruptEncoder poisons in the evaluated CIFAR settings, and two ImageNet-100 configurations remain incomplete. These results demonstrate the potential of semantic probing while identifying important limitations that must be resolved before claiming a general defense.
\end{abstract}

\begin{IEEEkeywords}
Contrastive learning, self-supervised learning, backdoor attacks, poisoned sample detection, data cleansing.
\end{IEEEkeywords}

\section{Introduction}\label{sec:introduction}
\IEEEPARstart{C}{ontrastive} learning (CL) has become a widely adopted paradigm for learning transferable representations from large-scale unlabeled data. Representative CL methods learn consistent representations across different augmented views of the same sample~\cite{Chen2020SimCLR,He2020MoCo,Grill2020BYOL}. The resulting encoder can be transferred to a variety of downstream tasks with limited labeled data. However, CL commonly relies on large, weakly curated datasets collected from public sources, creating an opportunity for an adversary to inject poisoned samples during pre-training. A backdoored encoder can preserve clean-data utility while causing a downstream classifier to predict an attacker-chosen target class for trigger-embedded inputs~\cite{Carlini2022Poisoning,Saha2022SSLBackdoor,Liu2022PoisonedEncoder}. Unlike supervised backdoor attacks, CL attacks must establish the trigger--target association through augmentation-driven representation learning without direct label supervision. This difference changes the learning dynamics and feature distributions of poisoned samples, limiting the transferability of defenses designed for supervised learning~\cite{Li2023CTRL,Li2024Difficulty}.

These challenges motivate defenses that intervene before a compromised encoder is transferred to downstream tasks. We therefore study training-data cleansing: detecting and removing poisoned samples from an unlabeled pre-training dataset before training a final encoder on the cleaned data. This setting differs from model-level detection and sanitization, as pursued by DECREE and SSL-Cleanse~\cite{Feng2023DECREE,Zheng2024SSLCleanse}. Existing data-centric defenses provide several useful directions. PatchSearch detects patch-based poisons by localizing candidate triggers and training a poison classifier~\cite{Tejankar2023PatchSearch}. Confusion Training and ASSET actively increase the separability between clean and poisoned samples through additional optimization~\cite{Qi2023Proactive,Pan2023ASSET}. AUTO mines relatively clean and poison-concentrated subsets from the untrusted training data and iteratively refines them as supervisory signals~\cite{Chen2025AUTO}. Although these methods demonstrate the feasibility of poisoned-sample cleansing, they also reveal trade-offs among trigger generality, feature separability, optimization cost, and reliance on trusted data.

A central challenge is the limited separability between clean and poisoned samples in the feature space learned from the poisoned dataset. Poisoned samples retain their original image content, while the backdoor is implanted as an additional trigger--target association. Existing attacks establish this association using visible patches, frequency-domain perturbations, augmentation-aware poison construction, or bi-level trigger optimization~\cite{Saha2022SSLBackdoor,Li2023CTRL,Zhang2024CorruptEncoder,Sun2024BLTO}. Consequently, poisoned samples may not form a distinct cluster in the original feature space, and a detection criterion tailored to one trigger type may not generalize to another~\cite{Pan2023ASSET,Li2024Difficulty}. Patch localization is effective for compact spatial triggers but is less suitable for distributed or optimized perturbations~\cite{Tejankar2023PatchSearch}. Optimization-based detectors can improve feature separability, but often require repeated training and reliable clean or poison-concentrated supervision~\cite{Qi2023Proactive,Pan2023ASSET,Chen2025AUTO}. These limitations motivate an active transformation that amplifies the feature differences between clean and poisoned samples before clustering.

Our key hypothesis is that a semantic probe can expose feature differences caused by an implanted backdoor association. We use a fixed, pre-trained probe associated with a defender-selected target class, which may differ from or coincide with the attacker's target class. When the two target classes differ, the probe-induced feature alignment may conflict with the trigger--target association already encoded in poisoned samples, producing different feature distributions for clean and poisoned data. This competition is a testable mechanism hypothesis rather than a secrecy assumption. The current detector evaluates the separability of probe-transformed features and does not explicitly measure the displacement between original and transformed features.

The detection pipeline first applies the pre-trained probe generator to a trusted reference set and jointly trains an encoder on the potentially poisoned unlabeled dataset and the probe-transformed reference samples using the same unsupervised CL objective. After training, the same probe is applied to every training sample, and we extract and $L_2$-normalize the backbone features of the probe-transformed training and reference images.

The probe-transformed training and reference features are jointly mapped to two dimensions with UMAP using cosine distance~\cite{McInnes2018UMAP}. After standardizing the training coordinates, we apply DBSCAN to identify dense clusters and noise points~\cite{Ester1996DBSCAN}. Under the assumption that poisoned samples are a minority, the largest non-noise cluster is treated as clean, whereas samples in the remaining clusters and noise are flagged as suspicious. The reference features influence the shared UMAP embedding but are not included in DBSCAN and do not directly determine which cluster is retained. The implemented detector is therefore reference-assisted during manifold construction and uses a largest-cluster rule for poisoned-sample detection.

The main contributions of this work are summarized as follows:
\begin{itemize}
    \item We introduce a reusable semantic probe for poisoned-sample detection to transform the feature distributions of clean and poisoned samples.
    \item We incorporate probe-transformed trusted reference samples into unsupervised encoder training and subsequent joint feature analysis without using their labels as supervision.
    \item We design a density-based detection rule that requires neither poison annotations nor a known poison ratio: the largest DBSCAN cluster is retained, while other clusters and noise points are flagged as suspicious.
    \item We identify the assumptions and failure modes of the current detector and define the end-to-end evaluations needed to assess post-cleansing attack success, clean accuracy, robustness, and computational cost.
\end{itemize}

\section{Related Work}\label{sec:relatedwork}
\subsection{Backdoor Attacks on Contrastive Learning}
Data-poisoning backdoor attacks on CL inject a small set of crafted samples into an unlabeled pre-training dataset to backdoor the learned encoder. The attack objective is twofold: a downstream classifier built on the backdoored encoder should predict the attacker-chosen target class for trigger-embedded inputs while retaining normal accuracy on clean inputs. Because the encoder is reused across downstream tasks, the implanted trigger--target association can persist after linear evaluation or task-specific fine-tuning~\cite{Carlini2022Poisoning}. SSL-Backdoor stamps a fixed patch trigger on images from the target class and exploits augmentation-based representation learning to associate the trigger with that class~\cite{Saha2022SSLBackdoor}. PoisonedEncoder optimizes poison construction using reference inputs and supports multiple target inputs or classes without controlling the victim's pre-training procedure~\cite{Liu2022PoisonedEncoder}.

Subsequent attacks broaden both the trigger space and the poison-generation strategy. CTRL injects an augmentation-resistant frequency-domain trigger and exploits representation invariance to achieve high attack success at a low poison ratio~\cite{Li2023CTRL}. CorruptEncoder analyzes the role of random cropping and constructs each poison by combining a target-class reference object, a trigger, and a background image so that contrastive views strengthen the desired association~\cite{Zhang2024CorruptEncoder}. BLTO formulates trigger generation as a bi-level optimization problem: the inner loop simulates the victim CL procedure, whereas the outer loop optimizes a trigger generator to keep trigger-injected samples close to target-class samples in the embedding space~\cite{Sun2024BLTO}. These attacks differ in trigger visibility, spatial locality, and interaction with data augmentation, making detectors based on a single trigger signature inherently limited.

More fundamentally, clean representation learning and backdoor injection are coupled through the same CL objective. Prior analysis shows that this coupling can make poisoned samples difficult to separate from clean samples in the learned feature space~\cite{Li2024Difficulty}. Consequently, defenses based only on unmodified features, training losses, or class-conditional statistics may transfer poorly across CL algorithms and attack mechanisms. Our method instead applies a controlled semantic probe and performs detection on the resulting probe-transformed features.

\subsection{Backdoor Defenses for Contrastive Learning}
Existing defenses can be broadly categorized as model-centric or data-centric. Model-centric defenses inspect a pre-trained encoder for backdoor behavior or sanitize the encoder after training. DECREE searches for a small perturbation that causes diverse inputs to produce similar features and uses the recovered perturbation to detect backdoored encoders~\cite{Feng2023DECREE}. SSL-Cleanse combines feature clustering, trigger inversion, and backdoor unlearning to detect and mitigate Trojans in self-supervised encoders~\cite{Zheng2024SSLCleanse}. These methods are applicable when only the pre-trained encoder is available, but they do not directly produce a cleaned pre-training dataset.

Data-centric defenses detect and filter poisoned samples before training a final model on the cleaned dataset. PatchSearch first trains an SSL encoder on the poisoned data, localizes candidate trigger patches, measures their ability to alter cluster assignments when pasted onto other images, and trains a poison classifier from highly suspicious samples~\cite{Tejankar2023PatchSearch}. Its patch-localization mechanism is effective for compact spatial triggers but does not naturally extend to distributed frequency-domain or input-dependent triggers. Confusion Training uses deliberately incorrect labels to amplify the difference between clean and poisoned samples~\cite{Qi2023Proactive}. Although designed for supervised learning, it motivates active feature separation rather than passive outlier detection.

ASSET extends active separation to supervised learning, SSL, and transfer learning through a nested offset optimization between an untrusted training set and a small clean base set~\cite{Pan2023ASSET}. Its clean base set provides a reference for separating poisoned samples, but a distribution mismatch between the base and training data can dominate the backdoor-related difference. AUTO avoids external clean data by mining a poison-concentrated positive set and a mostly clean negative set from the untrusted dataset, then bootstrapping the two sets to refine the supervision signal~\cite{Chen2025AUTO}. Our framework uses trusted data differently: probe-transformed reference samples are combined with the untrusted dataset during encoder training under the same unsupervised CL objective. During detection, the same probe is applied to the training samples, and the transformed training and reference features are jointly embedded with UMAP. The reference samples do not enter DBSCAN, and their labels are not used as supervision. This design avoids an additional active-separation optimization stage, but requires direct evaluation of its sensitivity to the reference distribution and the largest-cluster assumption.

\subsection{Representation-Based Anomaly Detection}
Feature-based analysis is a major direction in poisoned-sample detection. Spectral Signatures centers deep features and removes samples with large projections onto a dominant singular direction associated with poisoning~\cite{Tran2018Spectral}. SPECTRE strengthens spectral detection through robust covariance estimation, which can expose a weak poison signal at low poison ratios~\cite{Hayase2021SPECTRE}. Beatrix characterizes high-order activation correlations with Gram matrices and learns class-conditional clean statistics, enabling the detection of input-dependent triggers~\cite{Ma2023Beatrix}. These methods show that backdoor poisoning can leave detectable structure in learned features, but their supervised formulations commonly rely on class labels, predicted classes, or coherent within-class feature distributions.

These conditions are not directly available for an unlabeled CL dataset. Dataset-wide statistics can be dominated by semantic diversity, whereas partitioning samples with a backdoored encoder can preserve the trigger--target association that the detector seeks to identify~\cite{Li2024Difficulty}. We therefore analyze normalized backbone features after applying the semantic probe. UMAP builds a shared neighborhood graph from the probe-transformed training and reference features~\cite{McInnes2018UMAP}, and DBSCAN clusters the standardized training embeddings without requiring the number of clusters in advance~\cite{Ester1996DBSCAN}. The final decision uses cluster density and size: it retains the largest non-noise cluster rather than selecting the cluster closest to a reference centroid.

\section{Threat Model}\label{sec:threatmodel}
\subsection{Attacker's Goal and Capabilities}
Let $\mathcal{D}=\mathcal{D}_{b}\cup\mathcal{D}_{p}$ denote the unlabeled pre-training dataset, where $\mathcal{D}_{b}$ and $\mathcal{D}_{p}$ are the benign (clean) and poisoned subsets, respectively. The poison ratio is
\begin{equation}
    \rho=\frac{|\mathcal{D}_{p}|}{|\mathcal{D}|}.
    \label{eq:poisoningratio}
\end{equation}
The defender does not know $\rho$; we consider the conventional data-poisoning setting in which poisoned samples are a minority. The attacker selects a target class $y_t$, a trigger parameter $\tau$, and a trigger transformation $\mathcal{T}_{\tau}(\cdot)$. The trigger may be a visible patch, a distributed frequency-domain pattern, or an optimized input-dependent perturbation. Depending on the attack, poisoned samples may be generated from target-class images, reference objects, or attack-specific optimization, and are processed by the same pre-training pipeline as clean samples.

The attack has two objectives. First, a downstream classifier $g_{\phi}\circ f_{\theta}$ built on the backdoored encoder should predict $y_t$ for trigger-embedded inputs. Second, the encoder and downstream classifier should retain their clean-data utility. The attacker can inject or replace samples in $\mathcal{D}$ but cannot modify the defender's detection code, trusted reference set, or post-cleansing training procedure. We adopt a strong knowledge assumption: the attacker may know the CL algorithm, encoder architecture, augmentation pipeline, training hyperparameters, probe generator, probe target class, and complete cleansing algorithm, and may optimize poisons through a surrogate CL pipeline. The defense therefore does not rely on keeping the probe or its target class secret. We consider both $y_t\neq y_r$ and the adaptive same-target setting $y_t=y_r$, where $y_r$ denotes the defender-selected probe target class.

\subsection{Defender's Knowledge and Capabilities}
The defender has full access to the unlabeled pre-training dataset $\mathcal{D}$ and can train an encoder, extract backbone features, and apply input transformations. The defender does not know the class labels, poison annotations, trigger pattern, target class, attack type, or poison ratio. The detector must therefore operate without pre-specifying the number of poisoned samples or semantic clusters.

In addition, the defender holds a trusted reference set $\mathcal{R}$ containing clean images from one manually selected class $y_r$. We set its size to approximately 1\% of the pre-training corpus: 500 images for CIFAR-10 and CIFAR-100, and 1,300 images for ImageNet-100. The reference images may come from a different dataset and need not match the class distribution or data-collection process of $\mathcal{D}_{b}$; they only need a compatible visual modality and input format. Their labels are used solely to select the probe target class and never enter a supervised loss, clustering decision, or poison supervision.

Let $G$ denote the fixed semantic probe generator associated with class $y_r$. The generator is pre-trained once and reused across datasets and encoder settings. Before encoder training, the defender applies $G$ to every reference image and trains the encoder on $\mathcal{D}\cup G(\mathcal{R})$ using the same unsupervised CL objective and augmentation pipeline; the reference labels do not enter the loss. After training, $G$ is applied to every sample in $\mathcal{D}$, with each perturbation clipped to an $\ell_{\infty}$ radius of $8/255$. The backbone features of $G(\mathcal{D})$ and $G(\mathcal{R})$ are $L_2$-normalized and jointly embedded with UMAP. DBSCAN is fitted only to the standardized coordinates of the training samples. Ground-truth poison indices are used only to evaluate detection performance and never affect the clustering decision.

\subsection{Assumptions and Defense Objective}
We make two primary assumptions. First, $\mathcal{R}$ is clean and protected from poisoning or replacement. Second, after semantic probing, poisoned samples do not form the largest density-connected component, although their exact prevalence is unknown. The second assumption is stronger than simply requiring a low poison ratio: if clean samples split into several semantic clusters, the largest DBSCAN cluster may not cover the full clean distribution. We make no secrecy assumption about the probe generator, its target class, or the detection procedure. Instead, the relationship between the attack target $y_t$ and probe target $y_r$ is treated as an experimental factor; the same-target setting $y_t=y_r$ directly tests the method when the hypothesized competition between two target associations is absent.

Let $\delta(x_i)\in\{0,1\}$ be the cleansing decision for $x_i\in\mathcal{D}$, where $1$ denotes a suspicious sample. The estimated poisoned set and the cleaned dataset are
\begin{equation}
    \widehat{\mathcal{D}}_{p}=\{x_i\in\mathcal{D}:\delta(x_i)=1\},\qquad
    \widehat{\mathcal{D}}_{b}=\mathcal{D}\setminus\widehat{\mathcal{D}}_{p}.
    \label{eq:cleansingsets}
\end{equation}
The detection objective is to maximize recovery of $\mathcal{D}_{p}$ while minimizing the removal of clean samples. The end-to-end defense objective is to train a final encoder on $\widehat{\mathcal{D}}_{b}$ that achieves a low attack success rate on trigger-embedded inputs while preserving clean accuracy. We therefore evaluate both sample-level detection metrics---including precision, recall, and false-positive rate---and post-cleansing model metrics, including attack success rate and clean accuracy. A clustering metric alone is insufficient to establish defense effectiveness.
\section{Motivation and Key Observations}
\label{sec:motivation}

This section motivates our defense through a representative
contrastive backdoor setting. The observations provide mechanistic
evidence for the design rather than a claim that every attack--learner
combination exhibits identical geometry. We first examine why poisoned samples
can be difficult to distinguish from genuine target-class samples
using their original representations. We then analyze the underlying
target-semantic association induced by backdoor training and show
that applying a common semantic probe reveals additional behavioral
differences between benign and poisoned samples. These observations
motivate the probe-assisted manifold analysis introduced in
Section~\ref{sec:method}.


\subsection{Limitations of Passive Detection on Original Features}
\label{sec:original_feature}

A straightforward strategy for poisoned-sample cleansing is to
directly identify suspicious samples from the representation space
of a potentially backdoored encoder. Such a strategy implicitly
assumes that poisoned samples exhibit sufficiently distinctive
geometric patterns, such as isolated clusters or conventional
feature outliers~\cite{Chen2018ActivationClustering,Tran2018Spectral,Hayase2021SPECTRE}.
However, this assumption may not hold in contrastive backdoor
learning~\cite{Li2024Difficulty}.

We illustrate this issue using SSL-Backdoor on ImageNet-100 with a
SimCLR encoder~\cite{Saha2022SSLBackdoor,Chen2020SimCLR}. In this
representative run, as shown in
Fig.~\ref{fig:original_analysis}(a),
poisoned samples do not form a cleanly isolated anomaly cluster.
Instead, a substantial portion of them is embedded in the same local
representation region as benign samples from the attack target
class. In contrast, benign samples from other semantic classes are
distributed over different regions of the representation space.

\begin{figure}[!t]
    \centering
    \includegraphics[width=0.98\columnwidth]{figures/fig_original_analysis.png}
    \caption{Original-representation analysis under SSL-Backdoor on
    ImageNet-100 with a SimCLR encoder. (a) UMAP visualization of
    other-class benign, target-class benign, and poisoned samples.
    (b) Distributions of cosine similarity to the target-class
    centroid for the same three groups.}
    \label{fig:original_analysis}
\end{figure}

This observation indicates that the difficulty does not simply
arise from the choice of a particular clustering algorithm.
Rather, the original representation itself provides limited
information for distinguishing poisoned samples from genuine
target-class samples. Consequently, passive detection based solely
on static feature geometry can be unreliable in such a setting.


\subsection{Why Poisoned Samples Align with Target Semantics}
\label{sec:target_overlap}

The above phenomenon is closely related to the learning objective
of contrastive backdoors. Contrastive learning encourages different
augmented views of an instance to share consistent representations,
while backdoor poisoning additionally establishes an association
between an attacker-specified trigger and a target semantic concept.
As this association is repeatedly reinforced during poisoned
pre-training, trigger-related representations are encouraged to
align with the target semantic
region~\cite{Saha2022SSLBackdoor,Li2023CTRL,Zhang2024CorruptEncoder,Sun2024BLTO}.

Let $\mathbf{z}_p$ denote the normalized representation of a
poisoned sample and let $\boldsymbol{\mu}_t$ denote the centroid of
benign representations from the attack target class. The
target-semantic association can be characterized by

\begin{equation}
    s_t(\mathbf{z}_p)
    =
    \operatorname{cos}
    \left(
        \mathbf{z}_p,
        \boldsymbol{\mu}_t
    \right).
    \label{eq:target_similarity}
\end{equation}

As shown in Fig.~\ref{fig:original_analysis}(b), poisoned samples
exhibit substantially higher similarity to the target-class centroid
than benign samples from other classes. More importantly, their
similarity distribution is close to that of genuine target-class
benign samples. This result quantitatively supports the observation
in Fig.~\ref{fig:original_analysis}(a): the original representation
space captures the attack-induced target semantics, but provides
limited information for determining whether a target-aligned
representation originates from a genuine target-class sample or a
poisoned sample.

This motivates a different question: rather than asking whether a
sample is anomalous in the original feature space, can we actively
intervene on the input and examine how its representation responds?


\subsection{Diagnostic Probing Reveals Poison--Clean Differences}
\label{sec:probe_observation}

The above observation motivates us to move from passive feature
inspection to active diagnostic probing. Specifically, we apply the
same fixed semantic probe $\mathcal{P}_{\epsilon}(\cdot)$ to each sample and examine
how its representation responds. For an input $x_i$, we denote

\begin{equation}
    \mathbf{z}_i=f_{\theta}(x_i), \qquad
    \mathbf{z}_i^{p}=f_{\theta}(\mathcal{P}_{\epsilon}(x_i)),
    \label{eq:probe_representation}
\end{equation}

where both representations are $\ell_2$ normalized.

Interestingly, the same probe induces markedly different responses
for benign target-class and poisoned samples. As shown in
Fig.~\ref{fig:probe_response}, benign target-class samples undergo
substantially larger representation changes, whereas poisoned
samples exhibit a more constrained response. We characterize the
representation change as

\begin{equation}
    r_{\mathrm{rep}}(x_i)
    =
    1-\operatorname{cos}
    (\mathbf{z}_i,\mathbf{z}_i^{p}),
    \label{eq:response_magnitude}
\end{equation}

\begin{figure*}[!t]
    \centering
    \includegraphics[width=\textwidth]{figures/fig5_diagnostic_probe_final.pdf}
    \caption{Diagnostic-probe analysis of benign target-class and
    poisoned samples. The figure compares their probe-induced
    representation responses and local-neighborhood changes, which
    are used to motivate probe-assisted detection.}
    \label{fig:probe_response}
\end{figure*}

In addition to the difference in individual feature responses,
probing also affects the local neighborhood structures differently.
We measure neighborhood retention by

\begin{equation}
    r_{\mathrm{nbr}}(x_i)
    =
    \frac{
    |\mathcal{N}_k(\mathbf{z}_i)
    \cap
    \mathcal{N}_k(\mathbf{z}_i^{p})|
    }{k},
    \label{eq:neighbor_retention}
\end{equation}

Poisoned samples retain a larger fraction of their original
neighbors, while benign target-class samples undergo substantially
stronger neighborhood reorganization. This observation is
consistent with the hypothesis that the attacker-induced
trigger--target association constrains the response of poisoned
representations to the diagnostic semantic intervention.

The response magnitude and neighborhood-retention score are used here
only to analyze the probing mechanism; they are not used as detection
scores or concatenated with the features in the implemented detector.

More importantly, these different responses reshape the local
geometry of the representation space. Compared with the original
representations in Fig.~\ref{fig:original_analysis}(a),
the probe-transformed representations in
Fig.~\ref{fig:probe_response} exhibit more distinctive poisoned
structures. Therefore, diagnostic probing exposes information that
is difficult to obtain from static representations alone.


\subsection{Design Implications}
\label{sec:design_implications}

The above observations lead to three design implications. First,
probe-transformed representations should supplement passive inspection
because poisoned and genuine target-class samples can overlap in the
original feature space. Second, the probe-induced difference is
expressed through local geometry, motivating a neighborhood-preserving
manifold representation rather than a global linear projection. Third,
a small trusted reference set can shape the shared manifold without
directly assigning clean labels to training samples.

Accordingly, our method jointly embeds the probe-transformed training
and reference features with UMAP~\cite{McInnes2018UMAP}, but applies
DBSCAN~\cite{Ester1996DBSCAN} only to the embedded training samples.
The reference samples influence manifold construction without entering
the clustering decision or determining the retained cluster. The
complete framework and executable procedure are presented in
Section~\ref{sec:method}.
\section{Methodology}\label{sec:method}
\subsection{Framework Overview}
\label{sec:method_overview}

Our objective is to cleanse a potentially poisoned, unlabeled
pre-training dataset before training the encoder that will be released
for downstream use. The proposed framework consists of two training
stages separated by sample-level detection. In the first stage, we
train a diagnostic encoder on the untrusted dataset together with a
small probe-transformed trusted reference set. We then apply the same
fixed semantic probe to every training and reference sample, extract
normalized backbone representations, and construct a shared UMAP
manifold. DBSCAN is fitted only to the embedded training samples. The
largest non-noise cluster is retained, while all remaining clusters
and noise points are removed. In the second stage, a final encoder is
trained from scratch using only the cleansed training dataset. The
diagnostic encoder, reference set, and semantic probe are not used in
this final training stage.
These two training phases constitute the defense itself. The additional
encoder trained on the unfiltered poisoned corpus in
Section~\ref{sec:experimental_setup} is an evaluation control and is not
part of the cleansing procedure.

Let $N=|\mathcal{D}|$ and let
$\mathcal{R}=\{r_j\}_{j=1}^{m}$ denote the trusted reference set,
where
\begin{equation}
    m\approx0.01N.
    \label{eq:reference_budget}
\end{equation}
Specifically, we use $m=500$ for CIFAR-10 and CIFAR-100 and
$m=1{,}300$ for ImageNet-100.
All samples in $\mathcal{R}$ belong to the defender-selected semantic
class $y_r$. This class information is used only to construct
$\mathcal{R}$ and select the corresponding probe; it is never provided
to the contrastive objective or the clustering algorithm.

\subsection{Reference-Augmented Diagnostic Training}
\label{sec:diagnostic_training}

Let $\mathcal{P}_{\epsilon}$ denote a fixed semantic probe transformation
associated with the defender-selected reference class $y_r$, where
$\epsilon$ bounds the maximum input perturbation. The transformation is
implemented by a pretrained generator that remains frozen throughout
the defense and requires no knowledge of the attack trigger or target
class.

Before diagnostic training, the probe is applied to every reference
sample to form
\begin{equation}
    \mathcal{R}^{p}
    =
    \{\mathcal{P}_{\epsilon}(r_j):r_j\in\mathcal{R}\}.
    \label{eq:probed_reference_set}
\end{equation}
This construction deliberately introduces a defender-controlled,
backdoor-like association into the trusted reference set: the common
probe pattern plays a role analogous to a trigger, while the shared
reference semantics provide the associated target concept. Unlike
malicious poisoning, however, this association is introduced only to
train a diagnostic encoder and does not alter the untrusted samples or
the final cleansed model.

The diagnostic training set is
$\mathcal{S}=\mathcal{D}\cup\mathcal{R}^{p}$. Training samples and
probe-transformed reference samples use the same stochastic
augmentation distribution. For each $s\in\mathcal{S}$, two views
$a_1(s)$ and $a_2(s)$ are independently sampled from this distribution
and processed by the selected CL algorithm. The diagnostic encoder is
optimized as
\begin{equation}
    \theta^{d}
    =
    \arg\min_{\theta}
    \frac{1}{|\mathcal{S}|}
    \sum_{s\in\mathcal{S}}
    \ell_{\mathrm{CL}}
    \left(a_1(s),a_2(s);\theta\right),
    \label{eq:diagnostic_objective}
\end{equation}
where $\ell_{\mathrm{CL}}$ denotes the objective of the underlying
contrastive or self-supervised learner. No reference label, poison
annotation, or supervised auxiliary loss is used. Each epoch traverses
both $\mathcal{D}$ and $\mathcal{R}^{p}$ once, preventing the smaller
reference set from being repeatedly oversampled.

Because every element of $\mathcal{R}^{p}$ couples the same probe with
the same reference semantics, unsupervised representation learning is
repeatedly exposed to this controlled association. The resulting
diagnostic encoder can therefore reveal how training samples respond to
a competing backdoor-like semantic cue. This encoder is used only for
detection and is not the final encoder deployed after cleansing.

\subsection{Probe-Transformed Representation Extraction}
\label{sec:probe_feature_extraction}

After diagnostic training, we freeze the encoder and apply the same
probe transformation to every training and reference sample. Let
$h_{\theta^{d}}$ denote the backbone of the diagnostic encoder. The
representations used by the detector are
\begin{equation}
    \mathbf{z}_{i}^{p}
    =
    \frac{
        h_{\theta^{d}}(\mathcal{P}_{\epsilon}(x_i))
    }{
        \|h_{\theta^{d}}(\mathcal{P}_{\epsilon}(x_i))\|_2
    },
    \quad x_i\in\mathcal{D},
    \label{eq:training_probe_feature}
\end{equation}
and
\begin{equation}
    \mathbf{q}_{j}^{p}
    =
    \frac{
        h_{\theta^{d}}(\mathcal{P}_{\epsilon}(r_j))
    }{
        \|h_{\theta^{d}}(\mathcal{P}_{\epsilon}(r_j))\|_2
    },
    \quad r_j\in\mathcal{R}.
    \label{eq:reference_probe_feature}
\end{equation}
We use only the normalized backbone output of each probe-transformed
sample. The original representation and projection-head output are not
concatenated with this feature. Consequently, detection depends on the
geometry exposed by a common semantic intervention rather than on an
explicit original-to-probed displacement score.

\subsection{Reference-Assisted Shared Manifold Construction}
\label{sec:shared_manifold}

We concatenate the probe-transformed training and reference features
to form
\begin{equation}
    \mathbf{Z}
    =
    [\mathbf{z}_{1}^{p},\ldots,\mathbf{z}_{N}^{p},
      \mathbf{q}_{1}^{p},\ldots,\mathbf{q}_{m}^{p}]^{\top}.
    \label{eq:joint_feature_matrix}
\end{equation}
UMAP maps $\mathbf{Z}$ to a two-dimensional manifold using cosine
distance~\cite{McInnes2018UMAP},
\begin{equation}
    \mathbf{U}
    =
    \operatorname{UMAP}_{\mathrm{cos}}(\mathbf{Z})
    =
    [\mathbf{u}_{1},\ldots,\mathbf{u}_{N},
      \mathbf{v}_{1},\ldots,\mathbf{v}_{m}]^{\top},
    \label{eq:shared_umap}
\end{equation}
where $\mathbf{u}_i\in\mathbb{R}^{2}$ and
$\mathbf{v}_j\in\mathbb{R}^{2}$ are the embedded training and
reference coordinates, respectively. Joint fitting allows the trusted
reference features to influence neighborhood-graph construction and
the resulting low-dimensional geometry. It does not, however, assign
a clean label to nearby training samples.

Before density clustering, each coordinate of the training embedding
is standardized using statistics computed from
$\{\mathbf{u}_i\}_{i=1}^{N}$:
\begin{equation}
    \widetilde{u}_{ik}
    =
    \frac{u_{ik}-\mu_k}{\sigma_k},
    \qquad k\in\{1,2\}.
    \label{eq:umap_standardization}
\end{equation}
The reference coordinates are used to construct the shared manifold
but are excluded from this standardization and from the subsequent
clustering decision.

\subsection{Density-Based Sample Cleansing}
\label{sec:density_cleansing}

We apply DBSCAN~\cite{Ester1996DBSCAN} with radius parameter
$\varepsilon_{d}$ and minimum neighborhood size $n_{\min}$ to the
standardized training coordinates
$\{\widetilde{\mathbf{u}}_i\}_{i=1}^{N}$. Let
\begin{equation}
    \mathbf{c}=(c_1,\ldots,c_N)
    =
    \operatorname{DBSCAN}
    (\{\widetilde{\mathbf{u}}_i\}_{i=1}^{N};
    \varepsilon_d,n_{\min}),
    \label{eq:dbscan_assignment}
\end{equation}
where $c_i=-1$ denotes a noise point and nonnegative values denote
density-connected clusters. Define the set of valid cluster labels as
$\mathcal{C}=\{c_i:c_i\geq0\}$. If $\mathcal{C}=\varnothing$, the
procedure terminates without returning a cleansed dataset. Otherwise,
we retain the largest valid cluster,
\begin{equation}
    c^{\star}
    =
    \arg\max_{c\in\mathcal{C}}
    \sum_{i=1}^{N}\mathbb{I}[c_i=c].
    \label{eq:largest_cluster}
\end{equation}
The sample-level decision is then
\begin{equation}
    \delta(x_i)
    =
    \mathbb{I}[c_i\neq c^{\star}].
    \label{eq:cleansing_decision}
\end{equation}
Thus, every noise point and every sample belonging to a smaller
non-noise cluster is marked as suspicious. If DBSCAN returns exactly
one valid cluster, that cluster is retained and only its noise points
are removed. The no-valid-cluster condition above acts as a fail-closed
rule, preventing a degenerate clustering result from causing
dataset-wide deletion.

The decision rule does not use $\rho$, ground-truth poison indices,
attack-specific thresholds, or the distance to a reference centroid.
Its key structural assumption is instead that, after diagnostic
probing, poisoned samples do not constitute the largest
density-connected component.

\subsection{Final Encoder Training}
\label{sec:final_training}

After removing the detected samples, we obtain
$\widehat{\mathcal{D}}_{b}$ as defined in
(\ref{eq:cleansingsets}). A new encoder is initialized independently
and trained from scratch on this cleansed dataset:
\begin{equation}
    \theta^{\mathrm{final}}
    =
    \arg\min_{\theta}
    \frac{1}{|\widehat{\mathcal{D}}_{b}|}
    \sum_{x\in\widehat{\mathcal{D}}_{b}}
    \ell_{\mathrm{CL}}
    \left(a_1(x),a_2(x);\theta\right).
    \label{eq:final_training_objective}
\end{equation}
Neither the trusted reference set nor its probe-transformed counterpart
is included in this stage. The semantic probe and diagnostic encoder
are also discarded. This separation ensures that the final model is
trained only on the retained portion of the original pre-training
corpus and that the reference data affect deployment solely through
the preceding cleansing decision.

Algorithm~\ref{alg:cleansing} summarizes the complete executable
procedure. Ground-truth poison indices and attack-specific information
are absent from every step; they are used only after cleansing to
evaluate the returned decisions.

\begin{algorithm}[!t]
\caption{Reference-Assisted Poisoned-Sample Cleansing}
\label{alg:cleansing}
\begin{algorithmic}[1]
\Require Untrusted corpus $\mathcal{D}$; trusted set $\mathcal{R}$;
fixed probe $\mathcal{P}_{\epsilon}$; CL objective
$\ell_{\mathrm{CL}}$; UMAP parameters $\phi_U$; DBSCAN parameters
$\phi_D$
\Ensure Cleansed set $\widehat{\mathcal{D}}_b$, suspicious set
$\widehat{\mathcal{D}}_p$, and final encoder $f_{\theta^{\mathrm{final}}}$
\State $\mathcal{R}^{p}\gets
\{\mathcal{P}_{\epsilon}(r):r\in\mathcal{R}\}$
\State Independently initialize diagnostic encoder $f_{\theta^{d}}$
\State $\theta^{d}\gets
\operatorname{TrainSSL}(\mathcal{D}\cup\mathcal{R}^{p};
\ell_{\mathrm{CL}})$ using the same augmentations
\State $\mathbf{Z}_{D}\gets
\{\operatorname{norm}(h_{\theta^{d}}(\mathcal{P}_{\epsilon}(x))):
x\in\mathcal{D}\}$
\State $\mathbf{Z}_{R}\gets
\{\operatorname{norm}(h_{\theta^{d}}(\mathcal{P}_{\epsilon}(r))):
r\in\mathcal{R}\}$
\State $(\mathbf{U}_{D},\mathbf{U}_{R})\gets
\operatorname{UMAP}_{\mathrm{cos}}([\mathbf{Z}_{D};\mathbf{Z}_{R}];
\phi_U)$
\State $\widetilde{\mathbf{U}}_{D}\gets
\operatorname{Standardize}(\mathbf{U}_{D})$
\State $\mathbf{c}\gets
\operatorname{DBSCAN}(\widetilde{\mathbf{U}}_{D};\phi_D)$
\State $\mathcal{C}\gets\{c_i:c_i\geq0\}$
\If{$\mathcal{C}=\varnothing$}
    \State \textbf{return} \textsc{NoValidCluster}
\EndIf
\State $c^{\star}\gets
\arg\max_{c\in\mathcal{C}}\sum_i\mathbb{I}[c_i=c]$
\State $\widehat{\mathcal{D}}_p\gets
\{x_i\in\mathcal{D}:c_i\neq c^{\star}\}$
\State $\widehat{\mathcal{D}}_b\gets
\mathcal{D}\setminus\widehat{\mathcal{D}}_p$
\State Independently initialize final encoder
$f_{\theta^{\mathrm{final}}}$
\State $\theta^{\mathrm{final}}\gets
\operatorname{TrainSSL}(\widehat{\mathcal{D}}_b;
\ell_{\mathrm{CL}})$
\State \textbf{return} $\widehat{\mathcal{D}}_b$,
$\widehat{\mathcal{D}}_p$, and $f_{\theta^{\mathrm{final}}}$
\end{algorithmic}
\end{algorithm}

\section{Experimental Evaluation}\label{sec:experiment}
\subsection{Experimental Setup}
\label{sec:experimental_setup}

Following the evaluation structure commonly used in poisoned-sample
cleansing studies~\cite{Chen2025AUTO}, we assess the defense from two
complementary perspectives: sample-level detection and end-to-end
backdoor mitigation. The former measures whether poisoned samples are
removed without excessive loss of benign data, whereas the latter tests
whether an encoder trained from scratch on the cleansed corpus suppresses
the backdoor while preserving clean-data utility.

\subsubsection{Datasets and Trusted Reference Sets}
We conduct experiments on CIFAR-10, CIFAR-100, and ImageNet-100, covering
small-resolution benchmarks and a larger, more diverse pre-training
corpus. CIFAR-10 and CIFAR-100 each contain 50,000 training images. For
both datasets, the trusted reference set contains 500 images, corresponding
to 1\% of the training-set size. For ImageNet-100, we use a fixed reference
set of 1,300 images, which is approximately 1\% of the pre-training corpus.
All reference images belong to one defender-selected semantic class. Their
class labels are used only to construct the reference set and select the
semantic probe; they are never provided to the contrastive objective or the
clustering algorithm. The untrusted and reference samples use the same
stochastic augmentation distribution during diagnostic training.

\subsubsection{Attacks, Learners, and Architecture}
We evaluate three representative data-poisoning attacks with different
trigger constructions: the patch-based SSL-Backdoor attack~\cite{Saha2022SSLBackdoor},
the frequency-domain CTRL attack~\cite{Li2023CTRL}, and the
augmentation-aware CorruptEncoder attack~\cite{Zhang2024CorruptEncoder}.
The default poison ratios are 1\% for SSL-Backdoor and CTRL and 0.5\% for
CorruptEncoder. Unless otherwise specified, all encoders use ResNet-18.
SimCLR~\cite{Chen2020SimCLR} serves as the primary contrastive learner,
and MoCoV3 and BYOL~\cite{Grill2020BYOL} are used to evaluate transfer
across self-supervised objectives. SimCLR and MoCoV3 are evaluated on all
three datasets, while the required BYOL evaluation focuses on
ImageNet-100. All formal training runs use random seed 42.

\subsubsection{Three-Stage Evaluation Protocol}
Each attack--learner--dataset configuration follows the three stages
summarized in Table~\ref{tab:three_stage_protocol}. First, we train the
diagnostic encoder on the poisoned training corpus together with the
probe-transformed reference set and generate a set of suspicious sample
indices. Second, we train a standard poisoned encoder on the original
unfiltered corpus to obtain the no-defense clean accuracy and attack
success rate. Third, we remove the samples detected in the first stage and
train an independently initialized encoder from scratch on the retained
corpus. The reference set, semantic probe, and diagnostic encoder are not
used in the second or third stage. This separation prevents the diagnostic
model from being evaluated as the deployed model and isolates the effect
of data cleansing.

\begin{table}[!t]
    \centering
    \caption{Three-stage training protocol. The reference set is used only
    in diagnostic training.}
    \label{tab:three_stage_protocol}
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lccc}
        \toprule
        Dataset & Diagnostic & Poisoned & CleanPost \\
        & encoder & encoder & encoder \\
        \midrule
        CIFAR-10/100 & 500 epochs & 800 epochs & 800 epochs \\
        ImageNet-100 & 300 epochs & 500 epochs & 500 epochs \\
        \bottomrule
    \end{tabular}}
\end{table}

The second-stage poisoned encoder and the third-stage CleanPost encoder
use the same learner, architecture, augmentation pipeline, attack
configuration, and downstream evaluation protocol. Their only intended
difference is the removal of the suspicious indices before third-stage
training. For CIFAR experiments, every formal stage is rerun with the
500-image reference protocol so that the detection indices and downstream
models share a consistent code version and data configuration. Existing
ImageNet-100 runs use the 1,300-image reference protocol and are retained
only after configuration and artifact verification.

\subsubsection{Detection Configuration}
The semantic probe is pre-trained once, frozen, and reused across all
datasets, attacks, and learners. After diagnostic training, we extract the
$L_2$-normalized backbone representation of every probe-transformed
training and reference sample. The two sets are jointly embedded by UMAP
with cosine distance, two output dimensions, 15 neighbors,
$\texttt{min\_dist}=0.1$, and random state 0. We standardize only the
embedded training coordinates and apply DBSCAN with
$\varepsilon_d=1.0$ and $n_{\min}=15$. The largest non-noise cluster is
retained; all smaller clusters and noise points are removed. These
parameters are fixed before inspecting the ground-truth poison indices.

\subsubsection{Compared Defenses}
The representative baseline protocol includes ASSET~\cite{Pan2023ASSET},
AUTO~\cite{Chen2025AUTO}, and PatchSearch~\cite{Tejankar2023PatchSearch}
under a common SimCLR--ResNet-18 setting on ImageNet-100. Each applicable
baseline must output a cleansed dataset, after which a new encoder is
trained from scratch using the same downstream evaluation protocol as our
method. Because PatchSearch explicitly localizes spatial patches, it is
evaluated on SSL-Backdoor; unsupported distributed-trigger settings are
reported as not applicable rather than modifying the attacks to match the
baseline. Published values obtained under different poison ratios,
architectures, or training schedules are not mixed with our reproduced
comparisons.

\subsection{Evaluation Metrics and Reporting Protocol}
\label{sec:evaluation_metrics}

\subsubsection{Poisoned-Sample Detection}
Let TP and FN denote detected and missed poisoned samples, respectively,
and let FP and TN denote removed and retained benign samples. We report
the true-positive rate (TPR), false-positive rate (FPR), precision, and
F1 score:
\begin{equation}
    \mathrm{TPR}=\frac{\mathrm{TP}}{\mathrm{TP}+\mathrm{FN}},\qquad
    \mathrm{FPR}=\frac{\mathrm{FP}}{\mathrm{FP}+\mathrm{TN}},
    \label{eq:detection_rates}
\end{equation}
\begin{equation}
    \mathrm{Precision}=\frac{\mathrm{TP}}{\mathrm{TP}+\mathrm{FP}},\qquad
    \mathrm{F1}=\frac{2\,\mathrm{Precision}\,\mathrm{TPR}}
    {\mathrm{Precision}+\mathrm{TPR}}.
    \label{eq:detection_precision_f1}
\end{equation}
We additionally report the number and proportion of removed samples,
noise points, and non-noise DBSCAN clusters. Unlike score-based detectors
that naturally produce a ranking, the current largest-cluster rule outputs
a hard cleansing decision. We therefore do not report AUROC or AUPRC
unless a continuous, pre-specified anomaly score is introduced.

\subsubsection{End-to-End Backdoor Mitigation}
We evaluate the poisoned and CleanPost encoders using linear evaluation:
the encoder is frozen and only a linear classifier is trained on the
corresponding labeled evaluation split. Clean top-1 accuracy (ACC)
measures utility on unmodified test images. Attack success rate (ASR)
measures the fraction of triggered, non-target test inputs classified as
the attacker-selected target class. We report both $(\mathrm{ACC}_p,
\mathrm{ASR}_p)$ for the standard poisoned encoder and
$(\mathrm{ACC}_d,\mathrm{ASR}_d)$ for the encoder trained on the cleansed
corpus. Detection quality alone is not treated as evidence of a successful
defense; a valid result must reduce ASR while preserving clean accuracy.

For each learner--dataset pair, a clean-only encoder provides an attack
validity control. We apply the same three trigger transformations to this
encoder and report the resulting target prediction frequency as the
\emph{clean-model triggered target rate}, not as ASR. If a poisoned
encoder does not raise the target prediction frequency above this clean
control, the corresponding attack is marked ineffective and is not counted
as evidence of defense success.

\subsubsection{Reproducibility and Statistical Scope}
For every run, we retain the complete configuration, code revision,
environment manifest, dataset and checkpoint hashes, logs, suspicious
indices, per-sample decisions, and final metrics. Formal model training is
performed once with seed 42; consequently, we report the individual run
rather than mean and standard deviation and do not make claims of
statistical stability across training seeds. UMAP random-state experiments
are reported separately as clustering-sensitivity analyses and do not
substitute for independent model-training repetitions. We also record the
wall-clock time and peak GPU memory for diagnostic training, feature
extraction, manifold construction, clustering, and final retraining.

% Detection, mitigation, ablation, and efficiency results will be inserted
% only after the corresponding artifacts pass configuration and hash audits.
\section{Discussion and Limitations}\label{sec:discussion}
% TODO
\section{Conclusion}\label{sec:conclusion}
% TODO

\bibliographystyle{IEEEtran}
\bibliography{references}
\end{document}
